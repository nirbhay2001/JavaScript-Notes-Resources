/*
 JavaScript Timing Events
 --> 1. The window object allows execution of code at specified time intervals.
 2. These time intervals are called timing events.
 3. The two key methods to use with JavaScript:
        1. setTimeout(function, milliseconds) --> Executes a function, afterwaiting a specified number of milliseconds.
        2. setInterval(function, milliseconds) --> Same as setTimeout(), but repeats the execution of the function continuously.



    // 1. setTimeout()
    // 2. clearTimeout()
    // 3. setInterval()
    // 4. clearInterval()
*/