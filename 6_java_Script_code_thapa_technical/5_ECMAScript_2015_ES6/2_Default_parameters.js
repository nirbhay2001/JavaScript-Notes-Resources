/*
 Default Parameters
    Default function parameters allow named parameters to be initialized with deafult values if no value or undefined is passed.


*/

function mult(a,b=5){
    return a*b;
}

console.log(mul(3));